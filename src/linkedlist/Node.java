package linkedlist;

public class Node {
    Node next; // because a linked list has a pointer to the next node
    int data; // implementing a simple list of integers

    public Node(int data) {
        this.data = data;
    }
}
